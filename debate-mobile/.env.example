# Base URL of debate-api. Must be your Mac's LAN IP (not localhost) for
# anything other than the iOS simulator — find it with `make lan-ip` in
# debate-infra. Restart Metro after changing this (values are inlined).
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
